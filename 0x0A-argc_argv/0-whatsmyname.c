#include <stdio.h>
/**
 * main - Entry point of the code
 * @argc: Counter of commandline arguments
 * @argv: Array holding command line arguments
 * Return: Returns 0 as success
 */
int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
