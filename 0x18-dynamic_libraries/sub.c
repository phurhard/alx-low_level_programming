#include "main.h"
/**
 * sub - Subtracts two integers and prints the result
 * @a: user first input
 * @b: user second input
 * Return: Returns the value of subtraction
 *
 */
int sub(int a, int b)
{
	int subtract;
       
	subtract = a - b;
	return (subtract);
}
