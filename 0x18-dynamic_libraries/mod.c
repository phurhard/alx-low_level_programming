#include <stdio.h>
#include "main.h"
/**
 * mod - Takes the modulos of two intergers
 * @a: user first input
 * @b: user second input
 * Return: Returns the value of modulus
 *
 */
int mod(int a, int b)
{
	int modulus;

	modulus = a + b;
	return (modulus);
}
