#include "main.h"
/**
 * div - Divides two integers and prints the result
 * @a: user first input
 * @b: user second input
 * Return: Returns the value of division
 *
 */
int div(int a, int b)
{
	int divisor;

	divisor = a / b;
	return (divisor);
}
