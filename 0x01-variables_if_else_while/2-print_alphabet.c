#include <stdio.h>
/**
 * main - Entry point to print the alphabet in lowercase
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alpha;
	for (aplha='a'; alpha<='z'; alpha++)
		putchar(alpha);
	putchar('\n');
	return (0);
}
